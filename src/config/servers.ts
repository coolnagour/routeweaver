
// This file is no longer used to store hardcoded server configurations.
// Server configurations are now managed via the UI and stored in local storage.
// The ServerConfig type and schema are defined in src/types/index.ts.

export {};
